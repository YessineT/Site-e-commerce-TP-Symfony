{# templates/home/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}GameHub - Discover Your Next Favorite Game{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
{% endblock %}

{% block body %}
   <!-- Hero Section -->
    <section class="hero-section">
        <div class="container hero-content text-center">
            <h1 class="display-4 fw-bold mb-4">Discover Your Next Favorite Game</h1>
            <p class="lead mb-4">Join millions of players and explore our vast collection of premium and free-to-play games.</p>
            <div class="d-flex justify-content-center gap-3">
                <a href="{{ path('app_store') }}" class="btn btn-primary btn-lg">Browse Store</a>
                <a href="{{ path('app_about') }}" class="btn btn-outline-light btn-lg">Learn More</a>
            </div>
        </div>
    </section>

    <!-- Featured Games -->
    <section class="py-5">
        <div class="container">
            <h2 class="section-title mb-4">Featured Games</h2>
            <div class="row row-cols-1 row-cols-md-3 g-4">
                {% for game in featuredGames %}
                    <div class="col">
                        <div class="card h-100 game-card shadow-sm">
                            <div class="card-img-top-container" style="height: 200px; overflow: hidden;">
                                <img src="{{ asset('uploads/games/' ~ game.thumbnail) }}"
                                     class="img-fluid w-100 h-100 object-fit-cover scalable-image"
                                     alt="{{ game.title }} Cover">
                            </div>
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title text-truncate">{{ game.title }}</h5>
                                <p class="card-text text-muted mb-2">{{ game.genre.name }}</p>
                                <div class="mt-auto">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="rating">
                                            {% for i in 1..5 %}
                                                {% if i <= game.averageRating|round %}
                                                    <i class="fas fa-star text-warning"></i>
                                                {% elseif i == game.averageRating|round(0, 'ceil') and game.averageRating|round != game.averageRating %}
                                                    <i class="fas fa-star-half-alt text-warning"></i>
                                                {% else %}
                                                    <i class="far fa-star text-warning"></i>
                                                {% endif %}
                                            {% endfor %}
                                            <span class="ms-1">{{ game.averageRating|number_format(1) }}</span>
                                        </div>
                                        <span class="text-primary fw-bold">${{ game.price|number_format(2) }}</span>
                                    </div>
                                </div>
                            </div>
                            <a href="{{ path('app_game_about', {'id': game.id}) }}" class="stretched-link"></a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- Limited time Free Games Section -->
    <section class="py-5 bg-dark">
        <div class="container">
            <h2 class="section-title">Free This Week</h2>
            {% for freeGame in freeGamesLimited %}
                <div class="row">
                    <div class="col-md-6">
                        <div class="card bg-transparent border-0">
                            <div class="row g-0">
                                <div class="col-md-4">
                                    <img src="{{ asset('uploads/games/' ~ freeGame.thumbnail) }}" class="img-fluid rounded" alt="{{ freeGame.title }}">
                                </div>
                                <div class="col-md-8">
                                    <div class="card-body">
                                        <h3 class="card-title">{{ freeGame.title }}</h3>
                                        <p class="card-text">{{ freeGame.shortDescription }}</p>
                                        <p class="card-text"><small class="text-secondary">Free until {{ freeGame.freeUntil|date('F j') }}</small></p>
                                        <a href="{{ path('app_game_about', {'id': freeGame.id}) }}" class="btn btn-primary">Get It Now</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>

    <!-- Free Games -->
    <section class="py-5 bg-secondary-subtle text-black">
        <div class="container">
            <h2 class="section-title">Free Games</h2>
            {% for freeGame in freeGames %}
                <div class="row">
                    <div class="col-md-6">
                        <div class="card bg-transparent border-0">
                            <div class="row g-0">
                                <div class="col-md-4">
                                    <img src="{{ asset('uploads/games/' ~ freeGame.thumbnail) }}" class="img-fluid rounded" alt="{{ freeGame.title }}">
                                </div>
                                <div class="col-md-8">
                                    <div class="card-body">
                                        <h3 class="card-title">{{ freeGame.title }}</h3>
                                        <p class="card-text text-black-50">{{ freeGame.shortDescription }}</p>
                                        <p class="card-text"><small class="text-secondary">Free until {{ freeGame.freeUntil|date('F j') }}</small></p>
                                        <a href="{{ path('app_game_about', {'id': freeGame.id}) }}" class="btn btn-primary">Get It Now</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>


    <!-- News Section -->
    <section class="py-5">
        <div class="container">
            <h2 class="section-title">Latest News</h2>
            <div class="row">
                {% for newsItem in latestNews %}
                    <div class="col-md-4 mb-4">
                        <div class="game-card bg-secondary border-0 h-100">
                            <div class="card-img-top-container scalable-image bg-transparent border-0" 
                                style="height: 200px; overflow: hidden;">
                                <img src="{{ asset('uploads/news/' ~ newsItem.image) }}" 
                                    class="card-img-top" 
                                    alt="{{ newsItem.title }}" 
                                    style="width: 100%; height: 100%; object-fit: cover;">
                            </div>
                            <div class="card-body">
                                <h5 class="card-title">{{ newsItem.title }}</h5>
                                <a href="{{ path('app_news_show', {id: newsItem.id, slug: newsItem.slug}) }}" 
                                class="btn btn-sm btn-primary">Read More</a>
                            </div>
                            <div class="card-footer bg-transparent border-0"></div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="text-center mt-4">
                <a href="{{ path('app_news') }}" class="btn btn-outline-light">View All News</a>
            </div>
        </div>
    </section>


{% endblock %}
